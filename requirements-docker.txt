# Slim dependencies for the Docker demo backend.
# The backend serves precomputed embeddings — heavy ML packages (cornac,
# matplotlib, sentence-transformers) are not needed at runtime.
# torch is installed separately in Dockerfile (CPU-only, smaller download).
numpy<2.0.0
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
tqdm>=4.60
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
torch-geometric>=2.3
