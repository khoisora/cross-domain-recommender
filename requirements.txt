# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.2
pydantic-settings==2.1.0
python-multipart==0.0.6

# Database
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.0

# Redis
redis==5.0.1

# Background jobs
apscheduler==3.10.4

# ML - PyTorch
torch==2.1.2
torchvision==0.16.2

# ML - Graph
torch-geometric==2.3.1

# ML - Embeddings
sentence-transformers==2.7.0   # 2.2.2 incompatible with huggingface_hub>=0.16; 3.x requires torch>=2.4
transformers==4.45.2           # <4.46 required for torch 2.1.2 (register_pytree_node not in 2.1.2)

# ML - Retrieval
faiss-cpu==1.13.2
tensorflow-recommenders==0.7.7

# ML - Baselines
scikit-learn==1.5.2
scipy==1.13.1
# numpy version controlled below for scikit-surprise compatibility
# pandas version controlled below for merlin-core compatibility

# ML - Optional
xgboost==2.0.3

# HTTP clients
httpx==0.25.2
aiohttp==3.9.1
requests==2.32.5

# Scraping (fallback)
beautifulsoup4==4.12.2

# Utils
python-dotenv==1.0.0
tqdm==4.66.1
orjson==3.9.10

# Testing
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0
aiosqlite==0.19.0

# Logging
structlog==23.2.0

# ML - Data Processing
matplotlib==3.8.2
seaborn==0.13.0

# ML - Additional
joblib==1.3.2

# Recommendation Libraries (replacing custom implementations)
# Note: Python 3.12 compatibility - some libraries have version restrictions
numpy<2.0.0  # Required for scikit-surprise compatibility
scikit-surprise==1.1.4  # Python 3.12 compatible version
# implicit==0.7.3  # NOT compatible with Python 3.12, use scikit-surprise for MF instead

# Graph-based recommendations
torch-geometric==2.3.1

# Neural recommendation models
cornac==2.2.1

# DeepFM (LibRecommender)
LibRecommender==1.5.2
gensim==4.4.0
smart-open==7.5.1

# DeepAPF (RecBole-CDR) - Cross-domain recommendation
# Full recbole-cdr source is vendored at vendor/recbole_cdr/ (no pip install needed).
# Only the base recbole library is required as a pip dependency.

# Additional ML dependencies
setuptools==81.0.0

# OPTIONAL: Two-Tower (Merlin) - has dependency conflicts with pandas
# Uncomment these lines if you want to try installing Two-Tower
# Note: Requires pandas<1.6.0dev0 which may conflict with other packages
# merlin-models==23.12.0
# merlin-core==23.8.0
# merlin-dataloader==23.8.0
# tensorflow==2.21.0

